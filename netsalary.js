Function netpay =0.3;
 const tax rate =o.3
 const nhif rate =0.02;
 const nssfRate =0.01;

 const basicSalary= parseFloat(prompt('enter your basic salary:'));
 const allowance =parseFloat(prompt(prompt("Enter your total allowance")));

 const grosssalary = basicSalary+allowance

 const nssfDeduction =Math.min(grosssalary*nssfRate);
 const nssfDeduction =Math.min(grosssalary*nhifRate);
 const paye =Math.max(grosssalary-nhifDeduction-paye;
  
  
  console.log(window.alert(`NHIF deduction is: ${nhifDeduction}`));
  console.log(window.alert(`NHIF deduction is: ${nssfDeduction}`));

  console.log(window.alert(`PAYE: ${PAYE}`));
  console.log(window.alert(`The gross salary is: ${grosssalary}`));
  console.log(window.alert(`The net pay: ${nhifDeduction}`));
  )
  netpay();
  